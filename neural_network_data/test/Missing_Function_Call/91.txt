HOSTS =('proxy1', 'proxy')
COMMANDS =('uname -a', 'uptime')
        (ssh.stdout.readlines())
    ('--------------- ' + host + ' --------------- ')
            (ssh.stderr.readlines())
            (res)
