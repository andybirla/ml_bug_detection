app = (__name__)
ask = (app, "/reddit_reader")
    sess = ()
    ({'User-Agent': 'I am testing Alexa: nobi'})
    ("https://www.reddit.com/api/login/", data=user_pass_dict)
    (1)
    html = (url)
    data = (html.content.decode("utf-8"))
    titles = (listing['data']['title']) for listing in data['data']['children']]
    titles = '... ([i for i in titles])
("/")
    return (welcome_message)
("YesIntent")
    headlines = ()
    headline_msg = "The current world news headlines are (headlines)
    return (headline_msg)
("NooIntent")
    return (bye_text)
    (port=8000, debug=True)
