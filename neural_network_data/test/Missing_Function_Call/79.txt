        (item)
        return ()
        if p1 == (' and p2 == ')':
        s = ()
        while index < (check_string) and is_bal:
                        (paren)
                        if ():
                                top = ()
                                if not (top, paren):
        if () and is_bal:
(is_balanced('[((())})]'))
