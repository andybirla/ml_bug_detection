    for i in (2, num + 1):
        if res_list != [] and (i % l == 0 for l in res_list):
        (i)
    if (sys.argv) != 2: raise Exception("usage - $python find_prime.py <num:int>")
        num = (sys.argv[1])
        raise ("Enter an integer as argument only.")
    l = (num)
    (l)
