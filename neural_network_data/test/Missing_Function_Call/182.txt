connect = ("What is your admin  password?\n")
    connect = ("What is your admin password?\n")
conn = ('password_manager.db')
cursor_ = ()
    cursor = (command)
    command = 'INSERT INTO S(SERVICE,USERNAME,PASSWORD) VALUES("'+service_+'","'+username_+'","'+password_+'");'
    (command)
    ()
    (command)
    ()
    (service_ + " password updated successfully.")
    (command)
    ()
    (service_ + " deleted from the database successfully.")
    ("SELECT * from STORE")
    data = ()
    if (data) == 0:
        ('No Data')
            ("service = ", row[0])
            ("username = ", row[1])
            ("password = ", row[2])
            ()
    ("SELECT SERVICE from STORE where SERVICE = ?", (service_,))
    data = ()
    if (data) == 0:
        ('There is no service named %s' % service_)
