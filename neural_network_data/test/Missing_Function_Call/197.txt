    return (n) / factorial(n - r)
    return (n, r) / factorial(r)
    ('choose between operator 1,2,3')
    ('1) Factorial')
    ('2) Permutation')
    ('3) Combination')
    operation = ('\n')
        ('Factorial Computation\n')
                n = (input('\n Enter  Value for n '))
                ('Factorial of {} = {}'.format(n, factorial(n)))
                ('Invalid Value')
        ('Permutation Computation\n')
                n = (input('\n Enter Value for n '))
                r = (input('\n Enter Value for r '))
                ('Permutation of {}P{} = {}'.format(n, r, permutation(n, r)))
                ('Invalid Value')
        ('Combination Computation\n')
                n = (input('\n Enter Value for n '))
                r = (input('\n Enter Value for r '))
                ('Combination of {}C{} = {}'.format(n, r, combination(n, r)))
                ('Invalid Value')
    ()
