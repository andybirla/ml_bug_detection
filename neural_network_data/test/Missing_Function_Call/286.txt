dropbox = ("dropbox")
scripts = ("scripts")
dbfile =("database/maindatabase.db")
master_db = (dropbox, dbfile)
f = (scripts + '/output/student.txt', 'a')
tdate = ("%d-%m")
conn = (master_db)
cursor = ()
(loc_stmt)
    row = ()
    numbers =(snumber)
    postdata = (values)
    req = (url, postdata)
    ('Attempting to send SMS to ' + sname + ' at ' + snumber + ' on ' + tdate)
    ('Attempting to send SMS to ' + sname + ' at ' + snumber + ' on ' + tdate + '\n')
        response = (req)
        response_url = ()
            ('SMS sent!')
        ('Send failed!')
        (e.reason)
