if (sys.argv) != 2:
    raise ('right format for using this script: $python fractals.py <int:depth_for_fractal>')
myPen = ()
()
(5)
('red')
    r((p1[0] + p2[0]) / 2, (p1[1] + p2[1]) / 2)  # find midpoint
    ()
    (points[0][0], points[0][1])
    ()
    (points[1][0], points[1][1])
    (points[2][0], points[2][1])
    (points[0][0], points[0][1])
                  (points[0], points[1]),
                  (points[0], points[2])],
                  (points[0], points[1]),
                  (points[1], points[2])],
                  (points[2], points[1]),
                  (points[0], points[2])],
(points, int(sys.argv[1]))
