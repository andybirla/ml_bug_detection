    destLocation = (sys.argv[1])
    destLocation = (input('Enter the Path of directory: '))
        (dir)
        ('Error! Cannot change the Directory')
        ('Enter a valid directory!')
        (str(input('Enter the Path of directory: ')))
(destLocation)
        (name)
        ('{} Folder Created'.format(name))
        ('{} Folder Exist'.format(name))
    src = (destLocation, dirs)
    dest = (destLocation, name)
    (dest)
    (src, '{}\\{}'.format(dest, dirs))
    (os.getcwd())
    (destLocation)
for dirs in (os.getcwd()):
            if ('.')[-1].upper() in extensions_list:
                (dirs, name)
            (dirs, 'Folders')
('Done Arranging Files and Folder in your specified directory')
