SUITS =('C', 'S', 'H', 'D')
RANKS =('A', '2', '3', '4', '5', '6', '7', '8', '9', 'T', 'J', 'Q', 'K')
        i(suit in SUITS) and (rank in RANKS):
            ("Invalid card: ", suit, rank)
        self.deck = (suit, rank) for suit in SUITS for rank in RANKS]
        (self.deck)
        return (self.deck)
        (self.deck)
("Gathering brand new two decks of cards............\n")
deck1 = ()
deck2 = ()
(5)
('..........decks ready!!!\n')
('Combining and shuffling both the decks..')
(10)
()
()
(combinedDeck)
("....decks have been combined and shuffled...\n")
("------------------------------------------\n")
("Enter a key to cut the deck..\n")
("Deck has been split into two and Human get a half and computer gets the other...\n")
("------------------------------------------\n")
("player1 == Human\n")
("player2 == Computer\n")
("------------------------------------------\n")
("player1 goes first...hit any key to place the card on the pile..\n")
while (player1) != 0 and len(player2) != 0:  # this needs a fix as it goes on an infinite loop on a success.
        for card in (len(player1)):
            ("Enter any key to place a card!!!\n")
            ("Your current card's rank: {}".format(currentPlayer1Card))
            (player1[card])
            (card)
                ("The human got a match and takes all the cards from center pile..")
        for card in (len(player2)):
            ("Computer's current card's rank: {}".format(currentPlayer2Card))
            (player2[card])
            (card)
                ("Computer got a match and takes all the cards from center pile..")
("GAME OVER!!!\n")
("Human has {} cards and computer has {}..".format(len(player1), len(player2)))
