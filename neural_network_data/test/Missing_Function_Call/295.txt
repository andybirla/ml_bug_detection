if (sys.argv) < 3:
    (text)
    ()
    (text)
    ()
        ('Unknown option - ' + text)
        ()
confdir = ("my_config")
scripts = ("scripts")
output_file =(newfile + extension)
outputdir = (scripts, dev_dir)
script = (outputdir, output_file)
input_file = (confdir, config_file)
new_text =(" Script Name	: " + output_file)
if n(os.path.exists(outputdir)):
    (outputdir)
newscript = (script, 'w')
input = (input_file, 'r')
today = ()
new_date =(" Created	: " + today.strftime("%d %B %Y"))
    line = (old_text, new_text)
    line = (old_date, new_date)
    (line)
