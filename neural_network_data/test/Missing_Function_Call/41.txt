data = (open('data.json'))
engine = ()
voices = ('voices')
('voice', voices[0].id)
    (audio)
    ()
    r = ()
    with () as source:
        ('Listening...')
        (source, duration=1.5)
        audio = (source)
        ('Recognizing..')
        query = (audio, language='en-in')
        (f'User said: {query}\n')
        ('Say that again please...')
    word = ()
        ('Here is what I found in dictionary..')
        d = (str(e) for e in d)
        (d)
    elif (get_close_matches(word, data.keys())) > 0:
        x = (word, data.keys())[0]
        ans = ().lower()
            ('ok ' + 'It means..' + data[x])
            ("Word doesn't exist. Please make sure you spelled it correctly.")
            ("We didn't understand your entry.")
        ("Word doesn't exist. Please double check it.")
    ()
