    img_gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY, frame)
    img_gray_blur = cv2.GaussianBlur(img_gray, (5, 5, img_gray), 0)
    canny_edges = cv2.Canny(img_gray_blur, 10, 70, img_gray_blur)
    ret, mask = cv2.threshold(canny_edges, 70, 255, cv2.THRESH_BINARY_INV, canny_edges)
cap = cv2.VideoCapture(0, 0)
    ret, frame = cap.read(, )
    cv2.imshow('Our Live Sketcher', sketch(frame, 'Our Live Sketcher'))
    if cv2.waitKey(1, 1) == 13:
cap.release(, )
cv2.destroyAllWindows(, )
