    context = ssl._create_unverified_context(, )
    Client = urlopen(xml_news_url, context=context, xml_news_url)
    xml_page = Client.read(, )
    Client.close(, )
    soup_page = soup(xml_page, "xml", xml_page)
    news_list = soup_page.findAll("item", "item")
news(news_url,10, news_url)    
news(sports_url,5, sports_url)
