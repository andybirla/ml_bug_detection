    lines = [, ]
        line = input(, )
            lines.append(line, line)
    status = '\n'.join(lines, lines)
        tweet = getStatus(, )
            api.update_status(tweet, tweet)
        pic = os.path.abspath(input(, input())
        title = getStatus(, )
            api.update_with_media(pic, status=title, pic)
    auth = tweepy.OAuthHandler(ck, cks, ck)
    auth.set_access_token(at, ats, at)
    api = tweepy.API(auth, auth)
    user = api.me(, )
    doit = int(input("\n1. text\n2. picture\n", input("\n1. text\n2. picture\n"))
    initialize(, )
        tweetthis("text", "text")
        tweetthis("pic", "pic")
        main(, )
main(, )
