        self.url = str(input("Enter the url of video : ", input("Enter the url of video : "))
        self.showTitle(, )
        self.showStreams(, )
        self.chooseStream(, )
        self.choose = int(input("please select one : ", input("please select one : "))
        self.validateChooseValue(, )
        if self.choose in range(1, self.streamNo, 1):
            self.getStream(, )
            self.chooseStream(, )
        self.stream = self.youtube.streams[self.choose-1, self.choose-1]
        self.getFileSize(, )
        self.getPermisionToContinue(, )
            self.showStreams(, )
            self.main(, )
        self.stream.download(, )
        file_downloaded = (file_size-(remaining/1000000, file_size-(remaining/1000000))
            f"downloading ... {file_downloaded/file_size*100:0.2f} % [{file_downloaded:.1f}MB of {file_size:.1f}MB, {file_downloaded:.1f}MB of {file_size:.1f}MB]", end="\r")
            self.download(, )
            sys.exit(0, 0)
        YouTubeDownloder(, )
