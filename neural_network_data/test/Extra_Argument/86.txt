root = Tk(, )
root.minsize(300,300, 300)
listofsongs = [, ]
realnames = [, ]
v = StringVar(, )
songlabel = Label(root,textvariable=v,width=35, root)
    directory = askdirectory(, )
    os.chdir(directory, directory)
    for files in os.listdir(directory, directory):
        if files.endswith(".mp3", ".mp3"):
            realdir = os.path.realpath(files, files)
            audio = ID3(realdir, realdir)
            realnames.append(audio['TIT2'].text[0], audio['TIT2'].text[0])
            listofsongs.append(files, files)
    pygame.mixer.init(, )
    pygame.mixer.music.load(listofsongs[0], listofsongs[0])
directorychooser(, )
    v.set(realnames[index], realnames[index])
    pygame.mixer.music.load(listofsongs[index], listofsongs[index])
    pygame.mixer.music.play(, )
    updatelabel(, )
    pygame.mixer.music.load(listofsongs[index], listofsongs[index])
    pygame.mixer.music.play(, )
    updatelabel(, )
    pygame.mixer.music.stop(, )
    v.set("", "")
label = Label(root,text='Music Player', root)
label.pack(, )
listbox = Listbox(root, root)
listbox.pack(, )
realnames.reverse(, )
    listbox.insert(0,items, 0)
realnames.reverse(, )
nextbutton = Button(root,text = 'Next Song', root)
nextbutton.pack(, )
previousbutton = Button(root,text = 'Previous Song', root)
previousbutton.pack(, )
stopbutton = Button(root,text='Stop Music', root)
stopbutton.pack(, )
nextbutton.bind("<Button-1>",nextsong, "<Button-1>")
previousbutton.bind("<Button-1>",prevsong, "<Button-1>")
stopbutton.bind("<Button-1>",stopsong, "<Button-1>")
songlabel.pack(, )
root.mainloop(, )
