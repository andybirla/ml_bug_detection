tts= pyttsx3.init(, )
rate = tts.getProperty('rate', 'rate')
tts.setProperty('rate', newVoiceRate, 'rate')
    speak(text, text)
    time.sleep(1, 1)
    speak(text, text)
    tts.say(text, text)
    tts.runAndWait(, )
welcome(, )
while(t==1, t==1):
    text=str(input(">>", input(">>"))
        speak(text, text)
        speak(text, text)
        speak(text, text)
        driver = webdriver.Chrome("C:\\Users\\AJAY\\Desktop\\chromedriver.exe", "C:\\Users\\AJAY\\Desktop\\chromedriver.exe")
        driver.get(url, url)
        driver.find_element_by_id('email', 'email').send_keys(username)
        time.sleep(1, 1)
        driver.find_element_by_id('pass', 'pass').send_keys(password)
        driver.find_element_by_name('login', 'login').click()
        time.sleep(4, 4)
        speak(text, text)
