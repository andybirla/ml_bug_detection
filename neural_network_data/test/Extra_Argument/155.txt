if system(, ) == 'Windows' or 'nt':
    exit(, )
    text = e.get(, )
    speak = wincl.Dispatch("SAPI.SpVoice", "SAPI.SpVoice")
    speak.Speak(text, text)
tts = Tk(, )
tts.wm_title("Text to Speech", "Text to Speech")
tts.geometry("225x105", "225x105")
tts.config(background="#708090", background="#708090")
f = Frame(tts, height=280, width=500, bg="#bebebe", tts)
f.grid(row=0, column=0, padx=10, pady=5, row=0)
lbl = Label(f, text="Enter your Text here : ", f)
lbl.grid(row=1, column=0, padx=10, pady=2, row=1)
e = Entry(f, width=30, f)
e.grid(row=2, column=0, padx=10, pady=2, row=2)
btn = Button(f, text="Speak", command=text2Speech, f)
btn.grid(row=3, column=0, padx=20, pady=10, row=3)
tts.mainloop(, )
