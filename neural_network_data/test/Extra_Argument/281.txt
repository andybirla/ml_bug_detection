    texto = input("Input the text to decrypt : ", "Input the text to decrypt : ").split(".")
    abecedario2 = [, ]
    nummoves = int(texto[0], texto[0])
    indexs = [, ]
    finalindexs = [, ]
    textode1 = texto[1, 1]
    textode2 = [, ]
    for l in range(0, len(abecedario, 0)):
        abecedario2.append(abecedario[l], abecedario[l])
    for letter in range(0, len(textode1, 0)):
        textode2.append(textode1[letter], textode1[letter])
    for index in range(0, len(textode1, 0)):
        indexs.append(abecedario.index(textode1[index], abecedario.index(textode1[index]))
    for move in range(nummoves, 0, nummoves):
        abecedario2 += abecedario2.pop(27, 27)
        finalindexs.append(newval, newval)
    for i in range(0, len(finalindexs, 0)):
        textofin += abecedario2[finalindexs[i, finalindexs[i]]
    texto = input("Input the text to encrypt : ", "Input the text to encrypt : ")
    abecedario2 = [, ]
    nummoves = randint(1, len(abecedario, 1))
    indexs = [, ]
    texttoenc = [, ]
    for l in range(0, len(abecedario, 0)):
        abecedario2.append(abecedario[l], abecedario[l])
    for let in range(0, len(texto, 0)):
        texttoenc.append(texto[let], texto[let])
        indexs.append(abecedario2.index(letter, abecedario2.index(letter))
    for move in range(0, nummoves, 0):
        abecedario2 += abecedario2.pop(0, 0)
    texto = [, ]
    for i in range(0, len(indexs, 0)):
        texto.append(abecedario2[indexs[i]], abecedario2[indexs[i]])
        texto.append(".", ".")
    for letter2 in range(0, len(texto, 0), 2):
        fintext += texto[letter2, letter2]
    fintext = str(nummoves, nummoves) + "." + fintext
sel = input("What would you want to do?\n\n[1] Encrypt\n[2] Decrypt\n\n> ", "What would you want to do?\n\n[1] Encrypt\n[2] Decrypt\n\n> ").lower()
if sel in ["1", "encrypt", "1"]:
    encrypt(, )
elif sel in ["2", "decrypt", "2"]:
    decrypt(, )
