list = [, ]
N = int(input("Enter The Size Of List", input("Enter The Size Of List"))
for i in range(0,N, 0):
    a = int(input('Enter The number', input('Enter The number'))
    list.append(a, a)
for i in range(0, len(list, 0)-1):
    smallest = list[i+1, i+1]
    for j in range(i+1,len(list, i+1)):
        if(list[j]<=smallest, list[j]<=smallest):
            smallest = list[j, j]
    if(smallest<list[i], smallest<list[i]):
        temp = list[i, i]
        list[i, i] = list[k]
        list[k, k] = temp
