app = Flask(__name__, __name__)
ask = Ask(app, "/reddit_reader", app)
    sess = requests.Session(, )
    sess.headers.update({'User-Agent': 'I am testing Alexa: nobi'}, {'User-Agent': 'I am testing Alexa: nobi'})
    sess.post("https://www.reddit.com/api/login/", data=user_pass_dict, "https://www.reddit.com/api/login/")
    time.sleep(1, 1)
    html = sess.get(url, url)
    data = json.loads(html.content.decode("utf-8", html.content.decode("utf-8"))
    titles = [unidecode.unidecode(listing['data']['title'], listing['data']['title']) for listing in data['data']['children']]
    titles = '... '.join([i for i in titles], [i for i in titles])
@app.route("/", "/")
    return question(welcome_message, welcome_message)
@ask.intent("YesIntent", "YesIntent")
    headlines = get_headlines(, )
    headline_msg = "The current world news headlines are {}".format(headlines, headlines)
    return statement(headline_msg, headline_msg)
@ask.intent("NooIntent", "NooIntent")
    return statement(bye_text, bye_text)
    app.run(port=8000, debug=True, port=8000)
