engine = pyttsx3.init(, )
voices = engine.getProperty('voices', 'voices')
engine.setProperty('voice', voices[0].id, 'voice')
    engine.say(audio, audio)
    engine.runAndWait(, )
ans = requests.get(url, url)
soup = BeautifulSoup(ans.content, 'html.parser', ans.content)
anchors = soup.find_all('a', class_='cb-nws-hdln-ancr text-hvr-underline', 'a')
speak('Welcome to sports news headlines!', 'Welcome to sports news headlines!')
    speak(anchor.get_text(, anchor.get_text())
    speak('Moving on next sports headline..', 'Moving on next sports headline..')
speak('These all are major headlines, have a nice day SIR', 'These all are major headlines)
