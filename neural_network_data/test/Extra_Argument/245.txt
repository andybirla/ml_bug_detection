    os.system('clear', 'clear')
    logo(, )
    usr = input("Enter your Username : ", "Enter your Username : ")
    psw = getpass("Enter Your Password : ", "Enter Your Password : ")
    if(usr == usr1 and psw == psw1, usr == usr1 and psw == psw1):
        os.system('clear', 'clear')
        logo(, )
        login(, )
    login(, )
