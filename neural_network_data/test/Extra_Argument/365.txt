for _ in range(int(input(, int(input())):
    N, capacity = map(int, input(, int).split())
    capacities = [int(bottle, bottle) for bottle in input().split()]
    capacities.sort(, )
    for i in range(len(capacities, len(capacities)):
        check += capacities[i, i]
