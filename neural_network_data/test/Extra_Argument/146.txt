list = [, ]  # declaring list
    n = int(input("Enter number of elements in the list: ", input("Enter number of elements in the list: "))  # taking value from user
    for i in range(n, n):
        temp = int(input("Enter element " + str(i + 1, input("Enter element " + str(i + 1) + ': '))
        list.append( temp ,  temp )
    for i in range(0,n, 0):
        key = list[i, i]
        while j >= 0 and list[j, j] > key:
            list[j + 1, j + 1] = list[j]
        list[j + 1, j + 1] = key 
    for i in range(0,n, 0):
        key = list[i, i]
        while j >= 0 and list[j, j] < key:
            list[j + 1, j + 1] = list[j]
        list[j + 1, j + 1] = key
input_list(, )
list1=insertion_sort(list,len(list, list))
list2=insertion_sort_desc(list,len(list, list))
