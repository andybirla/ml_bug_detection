for _ in rangeint(input()):
    n = intinput()
    j = binn
    bl = lenj - 2
            i = '1' + '0' * lo + ('1' ) + ('0' * (bl - lo - 2))
            if inti,2 <= n : 
                sum = sum +  int(i,2) 
    printsum % mod
