    failure = []
    for index, char in enumerate():
        if pattern[] == char:
        failure.append()
    while i < len():
        if pattern[] == text[i]:
            if j == () - 1):
        elif failure[] > 0:
            j = failure[] - 1
    assert kmp( text1) and not kmp(pattern, text2)
    assert kmp( text)
