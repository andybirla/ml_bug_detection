for files in os.listdir():  # Find all the files in the directory
    if files.endswith():  # Check to ensure the files in the directory end in .log
        os.chdir()  # Change directory to the logsdir
        os.system()  # Zip the logs into dated zip files for each server. - 1.1
        os.remove()  # Remove the original log files
