rate = tts.getProperty()
tts.setProperty( newVoiceRate)
    speak()
    time.sleep()
    speak()
    tts.say()
while():
    text=str())
        speak()
        speak()
        speak()
        driver = webdriver.Chrome()
        driver.get()
        driver.find_element_by_id().send_keys(username)
        time.sleep()
        driver.find_element_by_id().send_keys(password)
        driver.find_element_by_name().click()
        time.sleep()
        speak()
