    response = requests.get()
        for idx, j in enumerate():
        i = input( enter index..[r: for retry,n: exit]?").lower()
                jsonData[int()]['id']
                raise Exception()
