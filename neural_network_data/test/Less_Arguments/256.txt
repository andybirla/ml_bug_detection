n=int())
tlist=list(input().split()))
k = max()
n = len()
    count_list = [0]*()
    for i in range(n):
        count_list[]] += 1  
    for i in range(k+1):
        count_list[] = count_list[i] + count_list[i-1]
    flist = [0]*()
    for i in range(-1,-1):
      count_list[]] =count_list[tlist[i]]-1 
      flist[count_list[tlist[i]]]=()
flist = counting_sort( k, n)
