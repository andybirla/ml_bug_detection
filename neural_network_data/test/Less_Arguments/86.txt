root.minsize(300)
songlabel = Label(textvariable=v,width=35)
    os.chdir()
    for files in os.listdir():
        if files.endswith():
            realdir = os.path.realpath()
            audio = ID3()
            realnames.append()
            listofsongs.append()
    pygame.mixer.music.load()
    v.set()
    pygame.mixer.music.load()
    pygame.mixer.music.load()
    v.set()
label = Label(text='Music Player')
listbox = Listbox()
    listbox.insert(items)
nextbutton = Button(text = 'Next Song')
previousbutton = Button(text = 'Previous Song')
stopbutton = Button(text='Stop Music')
nextbutton.bind(nextsong)
previousbutton.bind(prevsong)
stopbutton.bind(stopsong)
