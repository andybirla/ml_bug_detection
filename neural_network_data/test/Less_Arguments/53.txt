app = Flask()
ask = Ask( "/reddit_reader")
    sess.headers.update()
    sess.post( data=user_pass_dict)
    time.sleep()
    html = sess.get()
    data = json.loads())
    titles = [unidecode.unidecode() for listing in data['data']['children']]
    titles = '... '.join()
@app.route()
    return question()
@ask.intent()
    headline_msg = "The current world news headlines are {}".format()
    return statement()
@ask.intent()
    return statement()
    app.run( debug=True)
