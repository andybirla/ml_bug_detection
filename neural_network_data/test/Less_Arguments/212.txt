        os.system()  # Clear the Screen
    elif os.name in ( "dos", "ce"):  # DOS/Windows
        os.system()  # Clear the Screen
    subprocess.Popen()\Microsoft Office\Office14\winword.exe",
                      "/mFilePrintDefault", "/mFileExit"]).communicate().communicate()
    for server in open():
        subprocess.Popen())  # Open the PuTTY sessions - 1.1
    subprocess.Popen()  # Open up a terminal session connection and load the euroclear session
    filename = sys.argv[]  # Create the variable filename
    confdir = os.getenv()  # Set the variable confdir from the OS environment variable - 1.3
    conffilename = os.path.join( conffile)
          filename, "ran at", strftime(), "on", platform.node(), "run from", os.getcwd())
    putty_sessions()  # Call the putty_session function
