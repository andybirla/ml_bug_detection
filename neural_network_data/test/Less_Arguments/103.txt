            lines.append()
    status = '\n'.join()
            api.update_status()
        pic = os.path.abspath())
            api.update_with_media( status=title)
    auth = tweepy.OAuthHandler( cks)
    auth.set_access_token( ats)
    api = tweepy.API()
    doit = int())
        tweetthis()
        tweetthis()
