            acc_no = backend.create_customer( age, name, address, balance, acc_type, mobile_number)
            label = Label( text='Your account number is {}'.format(acc_no, create_employee_frame))
            button = Button( text="Exit", create_employee_frame, command=delete_create)
            label = Label( text='Please fill all entries', create_employee_frame)
            button = Button( text="Exit", create_employee_frame, command=delete_create)
    create_employee_frame = Frame( bg='black', tk)
    create_employee_frame.grid( pady=150, padx=500)
    label = Label( text='Customer Detail', create_employee_frame, font='bold')
    label.grid( pady=4, row=0)
    label = Label( text='Name', create_employee_frame, font='bold')
    label.grid( pady=4, row=1)
    entry5.grid( pady=4, row=2)
    label = Label( text='Age', create_employee_frame, font='bold')
    label.grid( pady=4, row=3)
    entry6.grid( pady=4, row=4)
    label = Label( text='address', create_employee_frame, font='bold')
    label.grid( pady=4, row=5)
    entry7.grid( pady=4, row=6)
    label = Label( text='Balance', create_employee_frame, font='bold')
    label.grid( pady=4, row=7)
    entry8.grid( pady=4, row=8)
    label = Label( text='Account Type', create_employee_frame, font='bold')
    label.grid( pady=4, row=9)
    label = Label( text='Mobile number', create_employee_frame, font='bold')
    label.grid( pady=4, row=11)
    entry9.grid( pady=4, row=10)
    entry10.grid( pady=4, row=12)
    button = Button( text='Submit', create_employee_frame, command=create_customer_in_database)
    button.grid( pady=4, row=13)
    search_frame.grid( pady=300, padx=500)
    label = Label( text="Enter account number", search_frame, font='bold')
    label.grid( pady=6, row=0)
    entry11.grid( pady=6, row=1)
    button = Button( text="Search", search_frame, command=show)
            show_frame.grid( pady=200, padx=400)
            label = Label( text="Account_number:\t{}".format(details[0], show_frame), font='bold')
            label.grid( pady=6, row=0)
            label = Label( text="Name:\t{}".format(details[1], show_frame), font='bold')
            label.grid( pady=6, row=1)
            label = Label( text="Age:\t{}".format(details[2], show_frame), font='bold')
            label.grid( pady=6, row=2)
            label = Label( text="Address:\t{}".format(details[3], show_frame), font='bold')
            label.grid( pady=6, row=3)
            label = Label( text="Balance:\t{}".format(details[4], show_frame), font='bold')
            label.grid( pady=6, row=4)
            label = Label( text="Account_type:\t{}".format(details[5], show_frame), font='bold')
            label.grid( pady=6, row=5)
            label = Label( text="Mobile Number:\t{}".format(details[6], show_frame), font='bold')
            label.grid( pady=6, row=6)
            button = Button( text='Exit', show_frame, command=clear_show_frame, width=20, height=2, bg='red', fg='white')
            button.grid( pady=6, row=7)
            label = Label( text="Account Not Found", search_frame)
            button = Button( text='Exit', search_frame, command=back_page2)
        label = Label( text="Enter correct account number", search_frame)
        button = Button( text='Exit', search_frame, command=back_page2)
                label = Label( text="invalid account number", search_frame)
                button = Button( text="Exit", search_frame, command=back_page2)
                    backend.update_balance( acc_no, new_money)
                add_frame.grid( pady=300, padx=400)
                label = Label( text='Account holder name:   {}'.format(detail[0][0], add_frame))
                label.grid( pady=3, row=0)
                label = Label( text='Current amount:   {}'.format(detail[0][1], add_frame))
                label.grid( pady=3, row=1)
                label = Label( text='Enter Money', add_frame)
                label.grid( pady=3, row=2)
                entry12.grid( pady=3, row=3)
                button = Button( text='Add', add_frame, command=update_money)
            label = Label( text="Enter correct account number", search_frame)
            button = Button( text="Exit", search_frame, command=back_page2)
        search_frame.grid( pady=300, padx=500)
        label = Label( text="Enter account number", search_frame, font='bold')
        label.grid( pady=6, row=0)
        entry11.grid( pady=6, row=1)
        button = Button( text="Search", search_frame, command=search_in_database)
                label = Label( text="invalid account number", search_frame)
                button = Button( text="Exit", search_frame, command=go_page2)
                    result = backend.deduct_balance( acc_no, new_money)
                        label = Label( text="Insufficient Balance", search_frame)
                        button = Button( text='Exit', search_frame, command=go_page2)
                add_frame.grid( pady=300, padx=400)
                label = Label( text='Account holder name:   {}'.format(detail[0][0], add_frame))
                label.grid( pady=3, row=0)
                label = Label( text='Current amount:   {}'.format(detail[0][1], add_frame))
                label.grid( pady=3, row=1)
                label = Label( text='Enter Money', add_frame)
                label.grid( pady=3, row=2)
                entry12.grid( pady=3, row=3)
                button = Button( text='Withdraw', add_frame, command=deduct_money)
            label = Label( text="Enter correct account number", search_frame)
            button = Button( text='Exit', search_frame, command=go_page2)
        search_frame.grid( pady=300, padx=500)
        label = Label( text="Enter account number", search_frame, font='bold')
        label.grid( pady=6, row=0)
        entry11.grid( pady=6, row=1)
        button = Button( text="Search", search_frame, command=search_in_database)
                label = Label( text="invalid account number", search_frame)
                button = Button( text="Exit", search_frame, command=back_page2)
                check_frame.grid( pady=300, padx=500)
                label = Label( text='Balance Is:{}'.format(balance, check_frame), font='bold')
                label.grid( pady=4, row=0)
                button = Button( text='Back', check_frame, command=delete_check_frame, width=20, height=2, bg='red')
            label = Label( text="Enter correct entry", search_frame)
            button = Button( text="Exit", search_frame, command=back_page2)
        search_frame.grid( pady=300, padx=500)
        label = Label( text="Enter account number", search_frame, font='bold')
        label.grid( pady=6, row=0)
        entry11.grid( pady=6, row=1)
        button = Button( text="Search", search_frame, command=search_in_database)
                    backend.update_name_in_bank_table( acc_no, new_name)
                    tkinter.messagebox.showinfo( 'Please fill blanks', 'Error')
            name_label = Label( text='Enter new name', update_customer_frame)
            name_label.grid( column=1, row=1)
            entry_name.grid( column=2, row=1, padx=2)
            submit_button = Button( text='Update', update_customer_frame, command=update_name_in_database)
            submit_button.grid( column=3, row=1)
                    backend.update_age_in_bank_table( acc_no, new_age)
                    tkinter.messagebox.showinfo( 'Please enter age', 'Error')
            age_label = Label( text='Enter new Age:', update_customer_frame)
            age_label.grid( column=1, row=2)
            entry_name.grid( column=2, row=2, padx=2)
            submit_button = Button( text='Update', update_customer_frame, command=update_age_in_database)
            submit_button.grid( column=3, row=2)
                    backend.update_address_in_bank_table( acc_no, new_address)
                    tkinter.messagebox.showinfo( 'Please fill address', 'Error')
            address_label = Label( text='Enter new Address:', update_customer_frame)
            address_label.grid( column=1, row=3)
            entry_name.grid( column=2, row=3, padx=2)
            submit_button = Button( text='Update', update_customer_frame, command=update_address_in_database)
            submit_button.grid( column=3, row=3)
                update_customer_frame.grid( pady=300, padx=300)
                label = Label( text='What do you want to update', update_customer_frame)
                name_button = Button( text='Name', update_customer_frame, command=update_name)
                name_button.grid( column=0, row=1, pady=6)
                age_button = Button( text='Age', update_customer_frame, command=update_age)
                age_button.grid( column=0, row=2, pady=6)
                address_button = Button( text='Address', update_customer_frame, command=update_address)
                address_button.grid( column=0, row=3, pady=6)
                exit_button = Button( text='Exit', update_customer_frame, command=back_to_page2_from_update)
                label = Label( text='Invalid account number', search_frame)
                button = Button( text='Exit', search_frame, command=back_to_page2)
            label = Label( text='Fill account number', search_frame)
            button = Button( text='Exit', search_frame, command=back_to_page2)
    search_frame.grid( pady=300, padx=500)
    label = Label( text='Enter account number', search_frame, font='bold')
    button = Button( text='update', search_frame, command=show_all_updateble_content, bg='red')
    list_frame.grid( pady=50, padx=50)
    label = Label( text="Acc_no\t\t\tName\t\t\tAge\t\t\tAddress\t\t\tbalance", list_frame)
        label = Label( text="{}\t\t\t{}\t\t\t{}\t\t\t{}\t\t\t{}".format(i[0], list_frame, i[1], i[2], i[3], i[4]))
    button = Button( text='Back', list_frame, width=20, height=2, bg='red', command=clear_list_frame)
                label = Label( text="invalid account number", search_frame)
                button = Button( text="Exit", search_frame, command=back_page2)
            label = Label( text="Enter correct account number", search_frame)
            button = Button( text="Exit", search_frame, command=back_page2)
        search_frame.grid( pady=300, padx=500)
        label = Label( text="Enter account number", search_frame, font='bold')
        label.grid( pady=6, row=0)
        entry11.grid( pady=6, row=1)
        button = Button( text="Delete", search_frame, command=search_in_database)
        frame = Frame( bg='black', tk)
        frame.grid( pady=250, padx=500)
        button = Button( text="Admin", frame, command=admin_login)
        button.grid( pady=20, row=0)
        button = Button( text="Employee", frame, command=employee_login)
        button.grid( pady=20, row=1)
        button = Button( text="Exit", frame, command=tk.destroy)
        button.grid( pady=20, row=2)
    frame1 = Frame( bg='black', tk)
    frame1.grid( pady=100, padx=500)
    button1 = Button( text="Create Account", frame1, command=create, width=20, height=2)
    button1.grid( pady=6, row=0)
    button2 = Button( text="Show Details", frame1, command=search_acc, width=20, height=2)
    button2.grid( pady=6, row=1)
    button3 = Button( text="Add balance", frame1, command=add, width=20, height=2)
    button3.grid( pady=6, row=2)
    button4 = Button( text="Withdraw money", frame1, command=withdraw, width=20, height=2)
    button4.grid( pady=6, row=3)
    button5 = Button( text="Check balance", frame1, command=check, width=20, height=2)
    button5.grid( pady=6, row=4)
    button6 = Button( text="Update Account", frame1, command=update, width=20, height=2)
    button6.grid( pady=6, row=5)
    button7 = Button( text="List of all members", frame1, command=allmembers, width=20, height=2)
    button7.grid( pady=6, row=6)
    button8 = Button( text="Delete Account", frame1, command=delete, width=20, height=2)
    button8.grid( pady=6, row=7)
    button9 = Button( text="Exit", frame1, command=back_to_main_from_page2, width=20, height=2)
    button9.grid( pady=6, row=8)
            backend.create_employee( password, name, salary, position)
            label = Label( text="Please fill all entries", frame_create_emp)
            button = Button( text="Exit", frame_create_emp, command=back_to_main_page1_from_create_emp, bg='red')
    frame_create_emp = Frame( bg='black', tk)
    frame_create_emp.grid( pady=200, padx=500)
    label = Label( text='Name:', frame_create_emp, font='bold')
    label.grid( pady=4, row=0)
    entry3.grid( pady=4, row=1)
    label2 = Label( text='Password', frame_create_emp, font='bold')
    label2.grid( pady=4, row=2)
    entry4.grid( pady=4, row=3)
    label3 = Label( text='Salary', frame_create_emp, font='bold')
    label3.grid( pady=4, row=4)
    entry16.grid( pady=4, row=5)
    label4 = Label( text='Position', frame_create_emp, font='bold')
    label4.grid( pady=4, row=6)
    entry17.grid( pady=4, row=7)
    button = Button( text='Submit', frame_create_emp, command=create_emp_in_database, width=15, height=2)
    button.grid( pady=4, row=8)
                        backend.update_employee_name( old_name, new_name)
                        tkinter.messagebox.showinfo( 'Please fill entry', 'Error')
                entry19.grid( column=1, row=1, padx=4)
                update_button = Button( text='Update', update_frame, command=database_calling)
                update_button.grid( column=2, row=1, padx=4)
                        backend.update_employee_password( old_name, new_password)
                        tkinter.messagebox.showinfo( 'Please Fill Entry', 'Error')
                entry19.grid( column=1, row=2, padx=4)
                update_button = Button( text='Update', update_frame, command=database_calling)
                update_button.grid( column=2, row=2, padx=4)
                        backend.update_employee_salary( old_name, new_salary)
                        tkinter.messagebox.showinfo( 'Invalid Input', 'Error')
                entry19.grid( column=1, row=3, padx=4)
                update_button = Button( text='Update', update_frame, command=database_calling)
                update_button.grid( column=2, row=3, padx=4)
                        backend.update_employee_position( old_name, new_position)
                        tkinter.messagebox.showinfo( 'Please Fill Entry', 'Error')
                entry19.grid( column=1, row=4, padx=4)
                update_button = Button( text='Update', update_frame, command=database_calling)
                update_button.grid( column=2, row=4, padx=4)
            update_frame.grid( pady=250, padx=400)
            label = Label( text='press what do you want to update', update_frame, font='bold')
            button = Button( text='Name', update_frame, command=update_name_in_database, width=14, height=2)
            button.grid( column=0, row=1, padx=2, pady=2)
            button = Button( text='password', update_frame, command=update_password_in_database, width=14, height=2)
            button.grid( column=0, row=2, padx=2, pady=2)
            button = Button( text='salary', update_frame, command=update_salary_in_database, width=14, height=2)
            button.grid( column=0, row=3, padx=2, pady=2)
            button = Button( text='position', update_frame, command=update_position_in_database, width=14, height=2)
            button.grid( column=0, row=4, padx=2, pady=2)
            button = Button( text='Back', update_frame, command=back_to_page1_from_update, width=14, height=2)
            button.grid( column=0, row=5, pady=2)
                label = Label( text='Employee not found', show_employee_frame)
                button = Button( text='Exit', show_employee_frame, command=back_to_page1)
            label = Label( text='Fill the name', show_employee_frame)
            button = Button( text='Exit', show_employee_frame, command=back_to_page1)
    show_employee_frame.grid( pady=300, padx=300)
    label = Label( text='Enter name of staff member whom detail would you want to update', show_employee_frame)
    update_butoon_for_staff = Button( text='Update Details', show_employee_frame, command=update_details_of_staff_member)
    show_employee_frame.grid( pady=50, padx=50)
    label = Label( text='Name\t\t\tSalary\t\t\tPosition\t\t\tpassword', show_employee_frame, font='bold')
        label = Label( text="{}\t\t\t{}\t\t\t{}\t\t\t{}".format(i[0], show_employee_frame, i[1], i[2], i[3]))
    all_money.grid( pady=300, padx=500)
    label = Label( text="Total Amount of money", all_money)
    label.grid( pady=6, row=0)
    label = Label( text='{}'.format(all, all_money))
    button = Button( text="Back", all_money, command=back_to_main_page1_from_total_money, width=15, height=2)
    frame = Frame( bg='black', tk)
    frame.grid( pady=250, padx=500)
    button = Button( text="Admin", frame, command=admin_login)
    button.grid( pady=20, row=0)
    button = Button( text="Employee", frame, command=employee_login)
    button.grid( pady=20, row=1)
    button = Button( text="Exit", frame, command=tk.destroy)
    button.grid( pady=20, row=2)
        frame = Frame( bg='black', tk)
        frame.grid( pady=250, padx=500)
        button = Button( text="Admin", frame, command=admin_login)
        button.grid( pady=20, row=0)
        button = Button( text="Employee", frame, command=employee_login)
        button.grid( pady=20, row=1)
        button = Button( text="Exit", frame, command=tk.destroy)
        button.grid( pady=20, row=2)
        result = backend.check_admin( password, name)
            page1_frame = Frame( bg='black', tk)
            page1_frame.grid( pady=200, padx=500)
            button10 = Button( text="New Employee", page1_frame, command=create_employee, width=20, height=2)
            button10.grid( pady=6, row=0)
            button11 = Button( text="Update detail", page1_frame, command=update_employee, width=20, height=2)
            button11.grid( pady=6, row=1)
            button13 = Button( text="Show All Employee", page1_frame, command=show_employee, width=20, height=2)
            button13.grid( pady=6, row=2)
            button11 = Button( text="Total Money", page1_frame, command=Total_money, width=20, height=2)
            button11.grid( pady=6, row=3)
            button12 = Button( text="Back", page1_frame, command=back_to_main, width=20, height=2)
            button12.grid( pady=6, row=4)
            label = Label( text="Invalid id and pasasword", admin_frame)
            label.grid( pady=10, row=6)
            button = Button( text='Exit', admin_frame, command=back_to_main2)
        label = Label( text="Please fill All Entries", admin_frame)
        label.grid( pady=10, row=6)
        button = Button( text='Exit', admin_frame, command=back_to_main2)
        frame = Frame( bg='black', tk)
        frame.grid( pady=250, padx=400)
        button = Button( text="Admin", frame, command=admin_login)
        button.grid( pady=20, row=0)
        button = Button( text="Employee", frame, command=employee_login)
        button.grid( pady=20, row=1)
        button = Button( text="Exit", frame, command=tk.destroy)
        button.grid( pady=20, row=2)
            result = backend.check_employee( password, name)
                label = Label( text="Invalid id and pasasword", employee_frame)
                label.grid( pady=10, row=6)
                button = Button( text='Exit', employee_frame, command=back_to_main3)
            label = Label( text="Please Fill All Entries", employee_frame)
            label.grid( pady=10, row=6)
            button = Button( text='Exit', employee_frame, command=back_to_main3)
    employee_frame = Frame( bg='black', tk)
    employee_frame.grid( pady=200, padx=500)
    label = Label( text="Employee Login", employee_frame, font='bold')
    label.grid( pady=20, row=0)
    label1 = Label( text="Name:", employee_frame)
    label1.grid( pady=10, row=1)
    label2 = Label( text="Password:", employee_frame)
    label2.grid( pady=10, row=3)
    entry1.grid( pady=10, row=2)
    entry2 = Entry( show='*', employee_frame)
    entry2.grid( pady=10, row=4)
    button = Button( text="Submit", employee_frame, command=check_emp)
    button.grid( pady=20, row=5)
    admin_frame = Frame( bg='black', tk)
    admin_frame.grid( pady=250, padx=500)
    label = Label( text="Admin Login", admin_frame, font='bold')
    label.grid( pady=20, row=0)
    label1 = Label( text="Name:", admin_frame)
    label1.grid( pady=10, row=1)
    label2 = Label( text="Password:", admin_frame)
    label2.grid( pady=10, row=3)
    entry1.grid( pady=10, row=2)
    entry2 = Entry( show='*', admin_frame)
    entry2.grid( pady=10, row=4)
    button = Button( text="Submit", admin_frame, command=page1)
    button.grid( pady=20, row=5)
tk.minsize( 800, 1200)
tk.maxsize( 800, 1200)
frame = Frame( bg='black', tk)
frame.grid( pady=250, padx=500)
button = Button( text="Admin", frame, command=admin_login)
button.grid( pady=20, row=0)
button = Button( text="Employee", frame, command=employee_login)
button.grid( pady=20, row=1)
button = Button( text="Exit", frame, command=tk.destroy)
button.grid( pady=20, row=2)
