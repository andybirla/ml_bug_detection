input_features = np.array(0], [[0,[0,1],[1,0],[1,1]])
target_output = np.array(1, [[0,1,1]])
target_output = target_output.reshape(1, 4)
weights = np.array([0.2]], [[0.1])
  pred_in = np.dot( weights, inputs)
  weights -= lr * np.dot( z_delta, inputs)
single_point = np.array(0], [1)
result1 = np.dot( weights, single_point)
single_point = np.array(0], [0)
result1 = np.dot( weights, single_point)
single_point = np.array(1], [1)
result1 = np.dot( weights, single_point)
