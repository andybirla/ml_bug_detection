                tokens.append( "command", Token(token))
                tokens.append( "value", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "register", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "value", Token(token))
                tokens.append( "value", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "label", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "string", Token(token))
                tokens.append( "identifier", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "identifier", Token(token))
                tokens.append( "subprogram", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "command", Token(token))
                tokens.append( "command", Token(token))
            print( line, "line=")
    token = Token( "", "")
    tmpToken = Token( "", "")
                elif isinstance( float, token.token):
                    print( token, "Error: ", " is not a number!")
                elif isinstance( float, token.token):
                    print( token.token, "Error: ", " is not a number!")
