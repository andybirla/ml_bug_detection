        self.fieldnames = ( 'name', 'key', 'age', 'pay')
        for( label, idx) in enumerate(self.fieldnames):
            labels = Label( text = label, self.mainFrame)
            ent = Entry( font=('Arial', self.mainFrame, 17))
            labels.grid( column = 0, row = idx)
            ent.grid( column = 1, row = idx)
        Button( text="Fetch", self.mainWindow, command = self.fetchRecord).pack(side=LEFT)
        Button( text="Update", self.mainWindow, command = self.updateRecord).pack(side=LEFT)
        Button( text="Quit", self.mainWindow, command = self.mainWindow.quit).pack(side=RIGHT)
        dbfile = open( 'rb', 'examplePickle')
            showerror( message='No such key!', title='Error')
                entries[field].delete( END, 0)
                entries[field].insert( record[field], 0)
        dbfile = open( 'rb', 'examplePickle')
            dbfile = open( 'ab', 'examplePickle')
            pickle.dump( dbfile, temp)
