class (TestCase):
    ('XOR_cipher.XORCipher.__init__')
        XORCipher.__init__ = ()
        ()
        ()
    ('XOR_cipher.XORCipher.encrypt')
        ans = ()
        content = ()
        key = ()
        XORCipher.encrypt = (return_value=ans)
        (content, key)
        (content, key)
    ('XOR_cipher.XORCipher.decrypt')
        ans = ()
        content = ()
        key = ()
        XORCipher.decrypt = (return_value=ans)
        (content, key)
        (content, key)
    ('XOR_cipher.XORCipher.encrypt_string')
        ans = ()
        content = ()
        key = ()
        XORCipher.encrypt_string = (return_value=ans)
        (content, key)
        (content, key)
    ('XOR_cipher.XORCipher.decrypt_string')
        ans = ()
        content = ()
        key = ()
        XORCipher.decrypt_string = (return_value=ans)
        (content, key)
        (content, key)
    ('XOR_cipher.XORCipher.encrypt_file')
        file = ()
        key = ()
        XORCipher.encrypt_file = (return_value=True)
        (file, key)
        (file, key)
    ('XOR_cipher.XORCipher.decrypt_file')
        file = ()
        key = ()
        XORCipher.decrypt_string = (return_value=True)
        (file, key)
        (file, key)
    ()
