    scanner = ()
    myIP = (['hostname -I'], shell=True)
    myIP = (myIP, 'utf-8').split('.')
    (myIP[:3])
    scannedData = (hosts = '.'.join(myIP[:3]) + '.1/24', arguments = '-sP')
        (hostnames)
()
