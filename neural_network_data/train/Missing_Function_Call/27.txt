    with (file_path,"r") as f:
        for line in ():
            tokens = (",")
            count += (tokens, num)
        if num == (token):
    with (file_path,"r") as f:
        for line in ():
            tokens = (",")
            total = (tokens)
            ("sum: " + str(total) + " | " + line)
    with (file_path,"w") as f:
        (output_lines)
        sum += (token)
("C:\\Code\\Py\\Basics\\input.txt")