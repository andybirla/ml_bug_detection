    compress_file = (file_path + '.zip', 'w')
    (path, compress_type=zipfile.ZIP_DEFLATED)
    ()
    for root, directories, files in (dir_name):
            file_path = (root, filename)
            (file_path)
    compress_dir = (dir_path + '.zip', 'w')
            (file)
    if (path):
        files_path = (path)
        ('The following list of files will be zipped:')
            (file_name)
        (path, files_path)
    elif (path):
        ('The %s will be zipped:' % path)
        (path)
        ('a special file(socket,FIFO,device file), please input file or dir')
