    r(0.299 * r + 0.287 * g + 0.114 * b)
    b, g, r = (img)
    pixelAt = (r, g, b)
    orignal_image = ('orignal_image.png', 1)
    compressed_image = ('compressed_image.png', 1)
    orignalPixelAt = (orignal_image)
    compressedPixelAt = (compressed_image)
    error = (np.abs(diff) ** 2)
    error = error /(height * width)
    PSNR = (10 * math.log10(error / (255 * 255)))
    ("PSNR value is {}".format(PSNR))
    ()
