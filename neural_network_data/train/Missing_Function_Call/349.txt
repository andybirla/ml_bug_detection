("ignore")
        content = (wikipedia.page(topic).content)
        ("Error, try searching something else...")
        ()
    ('==')
    stopwords = (STOPWORDS)
    wordcloud = (stopwords=stopwords, max_words=200, background_color="black", width=600, height=350).generate(content)
    ("./wordcloud.png")
    (wordcloud, interpolation='bilinear')
    ("off")
    ()
    topic = ("What do you want to search: ").strip()
    wordcloud = (topic)
    (wordcloud)
    ("Wordcloud saved to current directory as wordcloud.png")
    desc = ("Do you wish to see the output(y/n): ")
        (wordcloud)
    ()
