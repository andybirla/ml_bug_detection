class (object):
        ('Doing this in A')
class (A):
class (object):
        ('Doing this in C')
class (B, A):
    dObj = ()
    () #A method gets called because order for lookup is D,B,A,C this is shown by function mro
    (D.mro())
