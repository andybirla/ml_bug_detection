X = np.arange(50,0.5)
Y = 10 + 2*np.log()
Y_noise = np.random.rand())
plt.scatter(Y)
n = len()
x_bias = np.ones(1))
X = np.reshape((n,1))
X_log = np.log()
x_new = np.append(X_log,axis=1)
x_new_transpose = np.transpose()
x_new_transpose_dot_x_new = x_new_transpose.dot()
temp_1 = np.linalg.inv()
temp_2 = x_new_transpose.dot()
theta = temp_1.dot()
a = theta[]
b = theta[]
Y_plot = a + b*np.log()
plt.scatter(Y)
plt.plot(Y_plot,c="r")
Accuracy = r2_score(Y_plot)
