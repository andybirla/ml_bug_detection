filepath = sys.argv[]
if os.path.isdir():
    with open( "wb") as f:
        for fname in os.listdir():
            if not fname.endswith():
            path = os.path.join( fname)
            if os.path.isdir():
            imgs.append()
        f.write())
elif os.path.isfile():
    if filepath.endswith():
        with open( "wb") as f:
            f.write())
