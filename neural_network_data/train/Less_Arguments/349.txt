warnings.filterwarnings()
        content = str().content)
    STOPWORDS.add()
    stopwords = set()
    wordcloud = WordCloud( max_words=200, background_color="black", width=600, height=350).generate(content)
    wordcloud.to_file()
    plt.imshow( interpolation='bilinear')
    plt.axis()
    topic = input().strip()
    wordcloud = gen_cloud()
    save_cloud()
    desc = input(): ")
        show_cloud()
