    return parser.parse_args()
        with open( 'r') as userFile:
            userFileReader = csv.reader()
                data.append()
        with open( 'r', encoding="mbcs") as userFile:
            userFileReader = csv.reader()
                data.append()       
    with open( 'w') as newFile:
        newFileWriter = csv.writer()
        length = len()
        position = data[0].index()
        for i in range( length):
                _data = data[]
                _data.append()
                newFileWriter.writerow()
                __data = data[]
                    (), LANGUAGE, SENTENCES_COUNT)
                __data.append()
                newFileWriter.writerow()
        data = readCsv()
        writeCsv( LANGUAGE, SENTENCES_COUNT)
                        format='%()s:%(message)s')
    args = parse_args()
            processCsv( LANGUAGE, SENTENCES_COUNT)
        if os.path.isfile():
            return shutil.move( path)
            summarize( LANGUAGE, SENTENCES_COUNT)
