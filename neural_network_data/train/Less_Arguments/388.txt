class TestXORCipher():
    @mock.patch()
    @mock.patch()
        XORCipher.encrypt = mock.MagicMock()
        XORCipher.encrypt( key)
        XORCipher.encrypt.assert_called_with( key)
    @mock.patch()
        XORCipher.decrypt = mock.MagicMock()
        XORCipher.decrypt( key)
        XORCipher.decrypt.assert_called_with( key)
    @mock.patch()
        XORCipher.encrypt_string = mock.MagicMock()
        XORCipher.encrypt_string( key)
        XORCipher.encrypt_string.assert_called_with( key)
    @mock.patch()
        XORCipher.decrypt_string = mock.MagicMock()
        XORCipher.decrypt_string( key)
        XORCipher.decrypt_string.assert_called_with( key)
    @mock.patch()
        XORCipher.encrypt_file = mock.MagicMock()
        XORCipher.encrypt_file( key)
        XORCipher.encrypt_file.assert_called_with( key)
    @mock.patch()
        XORCipher.decrypt_string = mock.MagicMock()
        XORCipher.decrypt_string( key)
        XORCipher.decrypt_string.assert_called_with( key)
