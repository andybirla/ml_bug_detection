        if len() != 0 and len(age) != 0 and len(address) != 0 and len(balance) != 0 and len(acc_type) != 0 and len(
            acc_no = backend.create_customer( age, address, balance, acc_type, mobile_number)
            label = Label( text='Your account number is {}'.format(acc_no))
            label.grid()
            button = Button( text="Exit", command=delete_create)
            button.grid()
            label = Label( text='Please fill all entries')
            label.grid()
            button = Button( text="Exit", command=delete_create)
            button.grid()
    create_employee_frame = Frame( bg='black')
    create_employee_frame.grid( pady=150)
    label = Label( text='Customer Detail', font='bold')
    label.grid( pady=4)
    label = Label( text='Name', font='bold')
    label.grid( pady=4)
    entry5 = Entry()
    entry5.grid( pady=4)
    label = Label( text='Age', font='bold')
    label.grid( pady=4)
    entry6 = Entry()
    entry6.grid( pady=4)
    label = Label( text='address', font='bold')
    label.grid( pady=4)
    entry7 = Entry()
    entry7.grid( pady=4)
    label = Label( text='Balance', font='bold')
    label.grid( pady=4)
    entry8 = Entry()
    entry8.grid( pady=4)
    label = Label( text='Account Type', font='bold')
    label.grid( pady=4)
    label = Label( text='Mobile number', font='bold')
    label.grid( pady=4)
    entry9 = Entry()
    entry9.grid( pady=4)
    entry10 = Entry()
    entry10.grid( pady=4)
    button = Button( text='Submit', command=create_customer_in_database)
    button.grid( pady=4)
    search_frame = Frame()
    search_frame.grid( pady=300)
    label = Label( text="Enter account number", font='bold')
    label.grid( pady=6)
    entry11 = Entry()
    entry11.grid( pady=6)
    button = Button( text="Search", command=show)
    button.grid()
    r = check_string_in_account_no()
    if len() != 0 and r:
        details = backend.get_details()
            show_frame = Frame()
            show_frame.grid( pady=200)
            label = Label( text="Account_number:\t{}".format(details[0]), font='bold')
            label.grid( pady=6)
            label = Label( text="Name:\t{}".format(details[1]), font='bold')
            label.grid( pady=6)
            label = Label( text="Age:\t{}".format(details[2]), font='bold')
            label.grid( pady=6)
            label = Label( text="Address:\t{}".format(details[3]), font='bold')
            label.grid( pady=6)
            label = Label( text="Balance:\t{}".format(details[4]), font='bold')
            label.grid( pady=6)
            label = Label( text="Account_type:\t{}".format(details[5]), font='bold')
            label.grid( pady=6)
            label = Label( text="Mobile Number:\t{}".format(details[6]), font='bold')
            label.grid( pady=6)
            button = Button( text='Exit', command=clear_show_frame, width=20, height=2, bg='red', fg='white')
            button.grid( pady=6)
            label = Label( text="Account Not Found")
            button = Button( text='Exit', command=back_page2)
        label = Label( text="Enter correct account number")
        button = Button( text='Exit', command=back_page2)
        r = check_string_in_account_no()
        if len() != 0 and r:
            result = backend.check_acc_no()
                label = Label( text="invalid account number")
                label.grid()
                button = Button( text="Exit", command=back_page2)
                    backend.update_balance( acc_no)
                add_frame = Frame()
                add_frame.grid( pady=300)
                detail = backend.get_detail()
                label = Label( text='Account holder name:   {}'.format(detail[0][0]))
                label.grid( pady=3)
                label = Label( text='Current amount:   {}'.format(detail[0][1]))
                label.grid( pady=3)
                label = Label( text='Enter Money')
                label.grid( pady=3)
                entry12 = Entry()
                entry12.grid( pady=3)
                button = Button( text='Add', command=update_money)
                button.grid()
            label = Label( text="Enter correct account number")
            label.grid()
            button = Button( text="Exit", command=back_page2)
        search_frame = Frame()
        search_frame.grid( pady=300)
        label = Label( text="Enter account number", font='bold')
        label.grid( pady=6)
        entry11 = Entry()
        entry11.grid( pady=6)
        button = Button( text="Search", command=search_in_database)
        button.grid()
        r = check_string_in_account_no()
        if len() != 0 and r:
            result = backend.check_acc_no()
                label = Label( text="invalid account number")
                label.grid()
                button = Button( text="Exit", command=go_page2)
                    result = backend.deduct_balance( acc_no)
                        label = Label( text="Insufficient Balance")
                        label.grid()
                        button = Button( text='Exit', command=go_page2)
                        button.grid()
                add_frame = Frame()
                add_frame.grid( pady=300)
                detail = backend.get_detail()
                label = Label( text='Account holder name:   {}'.format(detail[0][0]))
                label.grid( pady=3)
                label = Label( text='Current amount:   {}'.format(detail[0][1]))
                label.grid( pady=3)
                label = Label( text='Enter Money')
                label.grid( pady=3)
                entry12 = Entry()
                entry12.grid( pady=3)
                button = Button( text='Withdraw', command=deduct_money)
                button.grid()
            label = Label( text="Enter correct account number")
            label.grid()
            button = Button( text='Exit', command=go_page2)
            button.grid()
        search_frame = Frame()
        search_frame.grid( pady=300)
        label = Label( text="Enter account number", font='bold')
        label.grid( pady=6)
        entry11 = Entry()
        entry11.grid( pady=6)
        button = Button( text="Search", command=search_in_database)
        button.grid()
        r = check_string_in_account_no()
        if len() != 0 and r:
            result = backend.check_acc_no()
                label = Label( text="invalid account number")
                label.grid()
                button = Button( text="Exit", command=back_page2)
                balance = backend.check_balance()
                check_frame = Frame()
                check_frame.grid( pady=300)
                label = Label( text='Balance Is:{}'.format(balance), font='bold')
                label.grid( pady=4)
                button = Button( text='Back', command=delete_check_frame, width=20, height=2, bg='red')
                button.grid()
            label = Label( text="Enter correct entry")
            label.grid()
            button = Button( text="Exit", command=back_page2)
        search_frame = Frame()
        search_frame.grid( pady=300)
        label = Label( text="Enter account number", font='bold')
        label.grid( pady=6)
        entry11 = Entry()
        entry11.grid( pady=6)
        button = Button( text="Search", command=search_in_database)
        button.grid()
                r = check_string_in_account_no()
                if len() != 0:
                    backend.update_name_in_bank_table( acc_no)
                    tkinter.messagebox.showinfo( 'Please fill blanks')
            name_label = Label( text='Enter new name')
            name_label.grid( column=1)
            entry_name = Entry()
            entry_name.grid( column=2, padx=2)
            submit_button = Button( text='Update', command=update_name_in_database)
            submit_button.grid( column=3)
                r = check_string_in_account_no()
                if len() != 0 and r:
                    backend.update_age_in_bank_table( acc_no)
                    tkinter.messagebox.showinfo( 'Please enter age')
            age_label = Label( text='Enter new Age:')
            age_label.grid( column=1)
            entry_name = Entry()
            entry_name.grid( column=2, padx=2)
            submit_button = Button( text='Update', command=update_age_in_database)
            submit_button.grid( column=3)
                if len() != 0:
                    backend.update_address_in_bank_table( acc_no)
                    tkinter.messagebox.showinfo( 'Please fill address')
            address_label = Label( text='Enter new Address:')
            address_label.grid( column=1)
            entry_name = Entry()
            entry_name.grid( column=2, padx=2)
            submit_button = Button( text='Update', command=update_address_in_database)
            submit_button.grid( column=3)
        r = check_string_in_account_no()
            result = backend.check_acc_no()
                update_customer_frame = Frame()
                update_customer_frame.grid( pady=300)
                label = Label( text='What do you want to update')
                label.grid()
                name_button = Button( text='Name', command=update_name)
                name_button.grid( column=0, pady=6)
                age_button = Button( text='Age', command=update_age)
                age_button.grid( column=0, pady=6)
                address_button = Button( text='Address', command=update_address)
                address_button.grid( column=0, pady=6)
                exit_button = Button( text='Exit', command=back_to_page2_from_update)
                exit_button.grid()
                label = Label( text='Invalid account number')
                button = Button( text='Exit', command=back_to_page2)
            label = Label( text='Fill account number')
            button = Button( text='Exit', command=back_to_page2)
    search_frame = Frame()
    search_frame.grid( pady=300)
    label = Label( text='Enter account number', font='bold')
    label.grid()
    entry_acc = Entry()
    entry_acc.grid()
    button = Button( text='update', command=show_all_updateble_content, bg='red')
    list_frame = Frame()
    list_frame.grid( pady=50)
    label = Label( text="Acc_no\t\t\tName\t\t\tAge\t\t\tAddress\t\t\tbalance")
    label.grid()
        label = Label( text="{}\t\t\t{}\t\t\t{}\t\t\t{}\t\t\t{}".format(i[0], i[1], i[2], i[3], i[4]))
        label.grid()
    button = Button( text='Back', width=20, height=2, bg='red', command=clear_list_frame)
        r = check_string_in_account_no()
        if len() != 0 and r:
            result = backend.check_acc_no()
                label = Label( text="invalid account number")
                label.grid()
                button = Button( text="Exit", command=back_page2)
                backend.delete_acc()
            label = Label( text="Enter correct account number")
            label.grid()
            button = Button( text="Exit", command=back_page2)
        search_frame = Frame()
        search_frame.grid( pady=300)
        label = Label( text="Enter account number", font='bold')
        label.grid( pady=6)
        entry11 = Entry()
        entry11.grid( pady=6)
        button = Button( text="Delete", command=search_in_database)
        button.grid()
        frame = Frame( bg='black')
        frame.grid( pady=250)
        button = Button( text="Admin", command=admin_login)
        button.grid( pady=20)
        button = Button( text="Employee", command=employee_login)
        button.grid( pady=20)
        button = Button( text="Exit", command=tk.destroy)
        button.grid( pady=20)
    frame1 = Frame( bg='black')
    frame1.grid( pady=100)
    button1 = Button( text="Create Account", command=create, width=20, height=2)
    button1.grid( pady=6)
    button2 = Button( text="Show Details", command=search_acc, width=20, height=2)
    button2.grid( pady=6)
    button3 = Button( text="Add balance", command=add, width=20, height=2)
    button3.grid( pady=6)
    button4 = Button( text="Withdraw money", command=withdraw, width=20, height=2)
    button4.grid( pady=6)
    button5 = Button( text="Check balance", command=check, width=20, height=2)
    button5.grid( pady=6)
    button6 = Button( text="Update Account", command=update, width=20, height=2)
    button6.grid( pady=6)
    button7 = Button( text="List of all members", command=allmembers, width=20, height=2)
    button7.grid( pady=6)
    button8 = Button( text="Delete Account", command=delete, width=20, height=2)
    button8.grid( pady=6)
    button9 = Button( text="Exit", command=back_to_main_from_page2, width=20, height=2)
    button9.grid( pady=6)
        if len() != 0 and len(password) != 0 and len(salary) != 0 and len(position) != 0:
            backend.create_employee( password, salary, position)
            label = Label( text="Please fill all entries")
            label.grid()
            button = Button( text="Exit", command=back_to_main_page1_from_create_emp, bg='red')
    frame_create_emp = Frame( bg='black')
    frame_create_emp.grid( pady=200)
    label = Label( text='Name:', font='bold')
    label.grid( pady=4)
    entry3 = Entry()
    entry3.grid( pady=4)
    label2 = Label( text='Password', font='bold')
    label2.grid( pady=4)
    entry4 = Entry()
    entry4.grid( pady=4)
    label3 = Label( text='Salary', font='bold')
    label3.grid( pady=4)
    entry16 = Entry()
    entry16.grid( pady=4)
    label4 = Label( text='Position', font='bold')
    label4.grid( pady=4)
    entry17 = Entry()
    entry17.grid( pady=4)
    button = Button( text='Submit', command=create_emp_in_database, width=15, height=2)
    button.grid( pady=4)
                    if len() != 0:
                        backend.update_employee_name( old_name)
                        tkinter.messagebox.showinfo( 'Please fill entry')
                entry19 = Entry()
                entry19.grid( column=1, padx=4)
                update_button = Button( text='Update', command=database_calling)
                update_button.grid( column=2, padx=4)
                    if len() != 0:
                        backend.update_employee_password( old_name)
                        tkinter.messagebox.showinfo( 'Please Fill Entry')
                entry19 = Entry()
                entry19.grid( column=1, padx=4)
                update_button = Button( text='Update', command=database_calling)
                update_button.grid( column=2, padx=4)
                    r = check_string_in_account_no()
                    if len() != 0 and r:
                        backend.update_employee_salary( old_name)
                        tkinter.messagebox.showinfo( 'Invalid Input')
                entry19 = Entry()
                entry19.grid( column=1, padx=4)
                update_button = Button( text='Update', command=database_calling)
                update_button.grid( column=2, padx=4)
                    if len() != 0:
                        backend.update_employee_position( old_name)
                        tkinter.messagebox.showinfo( 'Please Fill Entry')
                entry19 = Entry()
                entry19.grid( column=1, padx=4)
                update_button = Button( text='Update', command=database_calling)
                update_button.grid( column=2, padx=4)
            update_frame = Frame()
            update_frame.grid( pady=250)
            label = Label( text='press what do you want to update', font='bold')
            label.grid()
            button = Button( text='Name', command=update_name_in_database, width=14, height=2)
            button.grid( column=0, padx=2, pady=2)
            button = Button( text='password', command=update_password_in_database, width=14, height=2)
            button.grid( column=0, padx=2, pady=2)
            button = Button( text='salary', command=update_salary_in_database, width=14, height=2)
            button.grid( column=0, padx=2, pady=2)
            button = Button( text='position', command=update_position_in_database, width=14, height=2)
            button.grid( column=0, padx=2, pady=2)
            button = Button( text='Back', command=back_to_page1_from_update, width=14, height=2)
            button.grid( column=0, pady=2)
        if len() != 0:
            result = backend.check_name_in_staff()
                label = Label( text='Employee not found')
                button = Button( text='Exit', command=back_to_page1)
            label = Label( text='Fill the name')
            button = Button( text='Exit', command=back_to_page1)
    show_employee_frame = Frame()
    show_employee_frame.grid( pady=300)
    label = Label( text='Enter name of staff member whom detail would you want to update')
    staff_name = Entry()
    update_butoon_for_staff = Button( text='Update Details', command=update_details_of_staff_member)
    show_employee_frame = Frame()
    show_employee_frame.grid( pady=50)
    label = Label( text='Name\t\t\tSalary\t\t\tPosition\t\t\tpassword', font='bold')
    label.grid()
        label = Label( text="{}\t\t\t{}\t\t\t{}\t\t\t{}".format(i[0], i[1], i[2], i[3]))
        label.grid()
    all_money = Frame()
    all_money.grid( pady=300)
    label = Label( text="Total Amount of money")
    label.grid( pady=6)
    label = Label( text='{}'.format(all))
    label.grid()
    button = Button( text="Back", command=back_to_main_page1_from_total_money, width=15, height=2)
    button.grid()
    frame = Frame( bg='black')
    frame.grid( pady=250)
    button = Button( text="Admin", command=admin_login)
    button.grid( pady=20)
    button = Button( text="Employee", command=employee_login)
    button.grid( pady=20)
    button = Button( text="Exit", command=tk.destroy)
    button.grid( pady=20)
        frame = Frame( bg='black')
        frame.grid( pady=250)
        button = Button( text="Admin", command=admin_login)
        button.grid( pady=20)
        button = Button( text="Employee", command=employee_login)
        button.grid( pady=20)
        button = Button( text="Exit", command=tk.destroy)
        button.grid( pady=20)
    if len() != 0 and len(password) != 0:
        result = backend.check_admin( password)
            page1_frame = Frame( bg='black')
            page1_frame.grid( pady=200)
            button10 = Button( text="New Employee", command=create_employee, width=20, height=2)
            button10.grid( pady=6)
            button11 = Button( text="Update detail", command=update_employee, width=20, height=2)
            button11.grid( pady=6)
            button13 = Button( text="Show All Employee", command=show_employee, width=20, height=2)
            button13.grid( pady=6)
            button11 = Button( text="Total Money", command=Total_money, width=20, height=2)
            button11.grid( pady=6)
            button12 = Button( text="Back", command=back_to_main, width=20, height=2)
            button12.grid( pady=6)
            label = Label( text="Invalid id and pasasword")
            label.grid( pady=10)
            button = Button( text='Exit', command=back_to_main2)
            button.grid()
        label = Label( text="Please fill All Entries")
        label.grid( pady=10)
        button = Button( text='Exit', command=back_to_main2)
        button.grid()
        frame = Frame( bg='black')
        frame.grid( pady=250)
        button = Button( text="Admin", command=admin_login)
        button.grid( pady=20)
        button = Button( text="Employee", command=employee_login)
        button.grid( pady=20)
        button = Button( text="Exit", command=tk.destroy)
        button.grid( pady=20)
        if len() != 0 and len(password) != 0:
            result = backend.check_employee( password)
                label = Label( text="Invalid id and pasasword")
                label.grid( pady=10)
                button = Button( text='Exit', command=back_to_main3)
                button.grid()
            label = Label( text="Please Fill All Entries")
            label.grid( pady=10)
            button = Button( text='Exit', command=back_to_main3)
            button.grid()
    employee_frame = Frame( bg='black')
    employee_frame.grid( pady=200)
    label = Label( text="Employee Login", font='bold')
    label.grid( pady=20)
    label1 = Label( text="Name:")
    label1.grid( pady=10)
    label2 = Label( text="Password:")
    label2.grid( pady=10)
    entry1 = Entry()
    entry1.grid( pady=10)
    entry2 = Entry( show='*')
    entry2.grid( pady=10)
    button = Button( text="Submit", command=check_emp)
    button.grid( pady=20)
    admin_frame = Frame( bg='black')
    admin_frame.grid( pady=250)
    label = Label( text="Admin Login", font='bold')
    label.grid( pady=20)
    label1 = Label( text="Name:")
    label1.grid( pady=10)
    label2 = Label( text="Password:")
    label2.grid( pady=10)
    entry1 = Entry()
    entry1.grid( pady=10)
    entry2 = Entry( show='*')
    entry2.grid( pady=10)
    button = Button( text="Submit", command=page1)
    button.grid( pady=20)
tk.config()
tk.title()
tk.minsize( 800)
tk.maxsize( 800)
frame = Frame( bg='black')
frame.grid( pady=250)
button = Button( text="Admin", command=admin_login)
button.grid( pady=20)
button = Button( text="Employee", command=employee_login)
button.grid( pady=20)
button = Button( text="Exit", command=tk.destroy)
button.grid( pady=20)
