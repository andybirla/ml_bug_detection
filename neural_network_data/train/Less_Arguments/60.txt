    with webdriver.Chrome() as wd:
            wd.get()
        thumbnail_results = wd.find_elements_by_css_selector()
            if img.get_attribute() and 'http' in img.get_attribute('src'):
                images.append())
    target_folder = os.path.join('_'.join(search_term.lower().split(' ')))
        wd.execute_script( document.body.scrollHeight);")
        time.sleep()    
    wd.get())
        scroll_to_end()
        thumbnail_results = wd.find_elements_by_css_selector()
        number_results = len()
        for img in thumbnail_results[]:
                time.sleep()
            links = wd.find_elements_by_css_selector()
                if link.get_attribute() and 'http' in link.get_attribute('href'):
                    if link.get_attribute() not in d:
                        d[link.get_attribute()] = True
                        getactualurl = fetch_image_urls_util(),driver_path)
                            image_urls.add()
            image_count2 = len()
                        persist_image(elem)
        if len() >= max_links_to_fetch:
            time.sleep()
            load_more_button = wd.find_element_by_css_selector()
                wd.execute_script().click();")
        image_content = requests.get().content
        image_file = io.BytesIO()
        image = Image.open().convert('RGB')
        file_path = os.path.join(hashlib.sha1(image_content).hexdigest()[:10] + '.jpg')
        with open( 'wb') as f:
            image.save( "JPEG", quality=85)
    target_folder = os.path.join('_'.join(search_term.lower().split(' ')))
    if not os.path.exists():
        os.makedirs()
    with webdriver.Chrome() as wd:
        res = fetch_image_urls( number_images, wd=wd, sleep_between_interactions=0.5,driver_path= driver_path,target_path= target_path,search_term=search_term)
            persist_image(elem)
query = []
    search_and_download("./chromedriver.exe")