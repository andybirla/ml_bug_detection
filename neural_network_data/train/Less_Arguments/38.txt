s = socket.socket( socket.SOCK_STREAM)
s.bind(), 1023))
s.listen()
            reply = cli.recv().decode("utf-8")
                index_of_cli = clients.index()
                nick = nickename[]
                nickename.remove()
                clients.remove()
                BroadCasating()
            BroadCasating()
            index_of_cli = clients.index()
            nick = nickename[]
            nickename.remove()
            clients.remove()
            BroadCasating()
        client.send( "utf-8"))
        client_sckt.send( "utf-8"))
        nick = client_sckt.recv().decode("utf-8")
        nickename.append()
        clients.append()
        BroadCasating()
        threading._start_new_thread( (client_sckt , ))
