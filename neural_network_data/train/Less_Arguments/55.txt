font = ( 12, 'bold')
        tk.messagebox.showerror( "No File selected")
        img.save()
        tk.messagebox.showinfo( "File converted and saved")
root.title()
canvas1 = tk.Canvas( width=500, height=350, bg='lightblue')
label1 = tk.Label( text='PNG to ICO Converter', bg='lightblue')
label1.config( 20))
canvas1.create_window( 100, window=label1)
browseButton = tk.Button( command=getPNG, bg=bg, fg=fg, font=font, width=width)
canvas1.create_window( 150, window=browseButton)
saveAsButton = tk.Button( command=convertToICO, bg=bg, fg=fg, font=font, width=width)
canvas1.create_window( 200, window=saveAsButton)
