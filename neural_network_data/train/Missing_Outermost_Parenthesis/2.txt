def coin_flip:
    return random.randint0,1 
coin_flip
def monte_carlon:
    for i in rangen:
        flip_result = coin_flip
        prob_value = results/i+1
        list1.appendprob_value
        plt.axhliney=0.5, color='r', linestyle='-'
        plt.xlabel"Iterations"
        plt.ylabel"Probability"
        plt.plotlist1
answer = monte_carlo5000
print"Final value :",answer
