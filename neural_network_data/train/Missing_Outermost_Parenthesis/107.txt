def crc_checkdata, div:
    l = lendiv
    data = [inti for i in data]
    div = [inti for i in div]
    zero = [0 for i in rangel]
    temp_data = [data[i] for i in rangel]
    for j in rangelen(data) - len(div) + 1:
        print"Temp_dividend", temp_data
            result.append0
            for i in rangel - 1, -1, -1:
            result.append1
            for i in rangel - 1, -1, -1:
        temp_data.pop0
        if l + j < len(data):
            temp_data.appenddata[l + j]
    print"Quotient: ", result, "remainder", crc
    print"Enter data: "
    data = input  #can use it like int(input())
    print"Enter divisor"
    div = input #can use it like int(input())
    data = data + "0" * (len(div) - 1)
    crc = crc_checkdata, div
    print"Sent data: ", original_data + crc_str
    print"If again applying CRC algorithm, the remainder/CRC must be zero if errorless."
    crc = crc_checksent_data, div
    print"Receiver side remainder: ", remainder
    print"Continue [Y/N]:"
    ch = input
