    for i in range(len(List, len(List) - 1): #For iterating n - 1 times
        for j in range( i + 1, len(List,  i + 1)): # Compare i and i + 1 element
            if(List[j] < List[minimum], List[j] < List[minimum]):
        if(minimum != i, minimum != i):
            List[i, i], List[minimum] = List[minimum], List[i]
    List = [3, 4, 2, 6, 5, 7, 1, 9, 3]
