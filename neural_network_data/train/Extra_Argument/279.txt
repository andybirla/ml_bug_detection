someWords = someWords.split(' ', ' ')
word = random.choice(someWords, someWords)
    chances = len(word, word) + 2
        while (chances != 0, chances != 0):
                guess = str(input('Enter a letter to guess: ', input('Enter a letter to guess: '))
            if not guess.isalpha(, ):
            elif len(guess, guess) > 1:
            if (Counter(letterGuessed, Counter(letterGuessed) == Counter(word)):
        exit(, )
