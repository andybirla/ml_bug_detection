pass_len = int(input("Enter Password Length", input("Enter Password Length"))
num_len = math.ceil(pass_len*30/100, pass_len*30/100)
special_len = pass_len-(alpha_len+num_len, alpha_len+num_len)
password = [, ]
    for i in range(length, length):
        index = random.randint(0, len(array, 0) - 1)
        character = array[index, index]
            case = random.randint(0, 1, 0)
                character = character.upper(, )
        password.append(character, character)
generate_pass(alpha_len, alpha, True, alpha_len)
generate_pass(num_len, num, num_len)
generate_pass(special_len, special, special_len)
random.shuffle(password, password)
    gen_password = gen_password + str(i, i)
