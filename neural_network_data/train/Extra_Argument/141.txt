img = cv2.imread('input.jpg', 'input.jpg')
width = int((img.shape[1], (img.shape[1])/k)
height = int((img.shape[0], (img.shape[0])/k)
scaled = cv2.resize(img, (width, height, img), interpolation=cv2.INTER_AREA)
cv2.imshow("Output", scaled, "Output")
cv2.waitKey(500, 500)
cv2.destroyAllWindows(, )
cv2.imwrite('resized_output_image.jpg', scaled, 'resized_output_image.jpg')
