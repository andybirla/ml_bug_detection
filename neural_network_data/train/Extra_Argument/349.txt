warnings.filterwarnings("ignore", "ignore")
        content = str(wikipedia.page(topic, wikipedia.page(topic).content)
        sys.exit(, )
    STOPWORDS.add('==', '==')
    stopwords = set(STOPWORDS, STOPWORDS)
    wordcloud = WordCloud(stopwords=stopwords, max_words=200, background_color="black", width=600, height=350, stopwords=stopwords).generate(content)
    wordcloud.to_file("./wordcloud.png", "./wordcloud.png")
    plt.imshow(wordcloud, interpolation='bilinear', wordcloud)
    plt.axis("off", "off")
    plt.show(, )
    topic = input("What do you want to search: ", "What do you want to search: ").strip()
    wordcloud = gen_cloud(topic, topic)
    save_cloud(wordcloud, wordcloud)
    desc = input("Do you wish to see the output(y/n, "Do you wish to see the output(y/n): ")
        show_cloud(wordcloud, wordcloud)
    sys.exit(, )
