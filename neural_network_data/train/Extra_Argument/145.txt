    arr.sort(, )
    for i in range(0, n - 1, 2, 0):
        arr[i, i], arr[i + 1] = arr[i + 1], arr[i]
arr = [, ]
arr =input("Enter the arr", "Enter the arr")
sortInWave(arr, len(arr, arr))
for i in range(0, len(arr, 0)):
