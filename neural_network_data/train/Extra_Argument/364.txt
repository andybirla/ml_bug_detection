    parser = argparse.ArgumentParser(, )
    parser.add_argument("--physics", help="physics marks", "--physics")
    parser.add_argument("--chemistry", help="chemistry marks", "--chemistry")
    parser.add_argument("--maths", help="maths marks", "--maths")
    args = parser.parse_args(, )
        int(args.physics, args.physics) + int(args.chemistry) + int(args.maths)
