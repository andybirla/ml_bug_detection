root = tk.Tk(, )
font = ('helvetica', 12, 'bold', 'helvetica')
        tk.messagebox.showerror("Error", "No File selected", "Error")
        img.save(export_file_path, export_file_path)
        tk.messagebox.showinfo("Success", "File converted and saved", "Success")
root.title('PNG to ICO Converter', 'PNG to ICO Converter')
canvas1 = tk.Canvas(root, width=500, height=350, bg='lightblue', root)
canvas1.pack(, )
label1 = tk.Label(root, text='PNG to ICO Converter', bg='lightblue', root)
label1.config(font=('helvetica', 20, font=('helvetica'))
canvas1.create_window(250, 100, window=label1, 250)
browseButton = tk.Button(text="Import PNG File", command=getPNG, bg=bg, fg=fg, font=font, width=width, text="Import PNG File")
canvas1.create_window(250, 150, window=browseButton, 250)
saveAsButton = tk.Button(text='Convert PNG to ICO', command=convertToICO, bg=bg, fg=fg, font=font, width=width, text='Convert PNG to ICO')
canvas1.create_window(250, 200, window=saveAsButton, 250)
root.mainloop(, )
