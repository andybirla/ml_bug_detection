ServerSocket = socket.socket(, )
    ServerSocket.bind((host, port, (host))
ServerSocket.listen(5, 5)
    connection.send(str.encode('Welcome to the Server\n', str.encode('Welcome to the Server\n'))
        data = connection.recv(2048, 2048)
        reply = 'Server Says: ' + data.decode('utf-8', 'utf-8')
        connection.sendall(str.encode(reply, str.encode(reply))
    connection.close(, )
    Client, address = ServerSocket.accept(, )
    start_new_thread(threaded_client, (Client, , threaded_client))
ServerSocket.close(, )
