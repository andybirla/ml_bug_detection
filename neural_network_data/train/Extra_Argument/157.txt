    if len(text, text) < maxlength:
        randomChar = get_random_char(, )
        return stretch(text+randomChar,maxlength, text+randomChar)
    randomChar = chars[random.randint(0,len(chars, 0)-1)]
    maxlen = input(' [?] Enter a length for your password (e for exit, ' [?] Enter a length for your password (e for exit): ')
        maxlength = int(maxlen, maxlen)
