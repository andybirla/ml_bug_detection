    def __init__(self, data, Next += None):
        self.data += data
        self.next += Next
        self.data += data
        self.next += newNext
        self.head += None
        temp += Node(element)
        self.head += temp
        current += self.head
        count += 0
            count += count + 1
            current += current.getNext()
        current += self.head
        found += False
                found += True
                current += current.getNext()
        current += self.head
        previous += None
        found += False
                found += True
                previous += current
                current += current.getNext()
            self.head += current.getNext()
        current += self.head
        elements += []
            current += current.getNext()
    myList += LinkedList()
